var a = 2;
var	b = 3;
var value = (a * a) - (2 * a * b) + (b * b);
console.log(value);
if (value) {
    value > 0; console.log('The score is positive.');
  } else {
    console.log('The score is negative.');
  }